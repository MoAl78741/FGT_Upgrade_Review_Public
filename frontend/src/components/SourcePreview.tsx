import type {ReactNode} from 'react';
export default function SourcePreview({reference,children,onClose}:{reference:{url:string;page?:number;precision?:string};children:ReactNode;onClose:()=>void}){const url=new URL(reference.url,location.origin);const safe=url.origin===location.origin&&/^\/api\/jobs\/[^/]+\/files\/\d+$/.test(url.pathname);return <div role="dialog" aria-modal="true" aria-label="Source comparison" className="fixed inset-0 z-[100] bg-navy-900 p-4 flex flex-col gap-3"><div className="flex justify-between text-white"><p>Source comparison · {reference.page?`Section begins on page ${reference.page}; exact finding location is not recorded.`:'Page not recorded. Use PDF search to find the wording.'}</p><button onClick={onClose} autoFocus>Close comparison</button></div><div className="grid md:grid-cols-2 flex-1 min-h-0 gap-4"><section className="overflow-auto bg-navy-800 text-gray-300 p-4 rounded">{children}</section>{safe?<iframe title="Original source PDF" src={url.href} className="w-full h-full bg-white rounded"/>:<p>Source cannot be embedded. Use the original source link.</p>}</div></div>}
