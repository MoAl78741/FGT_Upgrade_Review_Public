export default function ExportPreview({html,onClose}:{html:string;onClose:()=>void}){return <div role="dialog" aria-modal="true" aria-label="Export preview" className="fixed inset-0 z-[100] bg-navy-900 p-4 flex flex-col gap-3"><div className="flex justify-between gap-3 text-white"><div><h2 className="font-semibold">Export preview</h2><p className="text-sm text-gray-400">Review the exact exported content. Download HTML, then open it and print to save a PDF.</p></div><button className="rounded border border-navy-600 px-4" onClick={onClose} autoFocus>Close preview</button></div><iframe title="Export content" sandbox="" srcDoc={html} className="w-full flex-1 bg-white rounded"/></div>}
