# ── Scraping (used by both CLI and web server) ────────────────────────────────
requests>=2.28.0
beautifulsoup4>=4.11.0

# ── Web server & API ──────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
pydantic>=2.0.0

# ── PDF upload mode ───────────────────────────────────────────────────────────
pdfplumber>=0.10.0
pymupdf4llm>=0.1.17
python-multipart>=0.0.9

# ── Selenium mode (--selenium flag, requires Chrome + ChromeDriver) ───────────
selenium>=4.0.0

# Preserve source formatting through dashboard and exports
markdownify>=1.2.3
